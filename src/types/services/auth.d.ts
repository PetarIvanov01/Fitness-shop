export type LoginParamsType = {
    email: string;
    password: string;
};
export type RegisterParamsType = {
    email: string;
    firstName: string;
    lastName: string;
    password: string;
    phoneNumber: string;
    rePassword: string;
};
